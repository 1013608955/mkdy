# mkdy 项目深度审查报告

> 审查时间：2026-08-17
> 范围：订阅管道（`fetch_tuijian.py` / `update_nodes.py` / `fetch.py` / `merge_subs.py` / `clash_template.yaml`）、验证管道（`verify_cn/run_local.py` / `tag_verified.py`）、容器调度（`verify_cn/set_schedule.sh` / `cloud_init.sh`）、CI 工作流（`.github/workflows/*.yml`）。
> 方法：逐文件静态阅读 + 跨文件引用/一致性核对 + 密钥模式扫描 + gitignore 核对。
> 结论：核心逻辑健壮、边界处理到位（尤其 `run_local.py` / `tag_verified.py`）；主要问题集中在**重复代码、废弃代码未清理、若干边界/标签不一致、以及少量安全 hygiene**。

---

## 1. 架构与数据流（复查确认）

```
源站抓取
  fetch_tuijian.py  → latest_tuijian.txt (孤儿,见 L-低-3)
  update_nodes.py   → s1.txt   (base64 URI, 已评分筛选)
  fetch.py          → s2.txt + s2-clash-1/2.yaml
        │
        ▼  update-subs.yml (每小时, 已修推送竞争)
  merge_subs.py 读取 s.txt/s1.txt/s2.txt + s2-clash-*.yaml
        → 去重 → 套 clash_template.yaml → s-clash.yaml (完整配置)
        │
        ▼  容器 run_local.py (每15分钟, mihomo 真链探测)  ★当前活动验证路径
  verified.json → push → verify-tag.yml 监听 push → tag_verified.py → s-verified.yaml
```
**已确认废弃（不再使用）的路线**：华为云 FunctionGraph（`index.py` / `verify_proxy_core.py` / `build_package.py` / `_cdp_*.py` / `s.yaml` / 各类 json/txt 产物），现由 `run_local.py`(mihomo) 取代。

---

## 2. 问题清单（按严重程度）

### 🔴 严重 / Critical
无。未发现会导致数据丢失、密钥已明文泄漏或线上崩溃的致命缺陷。

### 🟠 高 / High

**H1. 协议解析逻辑双份实现，需手动保持同步（未来 bug 温床）**
- 位置：`update_nodes.py` `PROTO_PARSERS`(L407-413) + `parse_vmess/vless/trojan/ss/hysteria`(L304-405)；`merge_subs.py` `PARSERS`(L311-320) + `parse_*`(L104-308)。
- 影响：两处各自把 `vmess://` 等 URI 解析成 dict，但字段语义不同（前者为评分/探测，后者为 Clash 输出）。任一协议字段变更（如新增 reality 子参数、tuic 字段）需同时改两处，极易漂移遗漏，导致「能进评分但合并后字段缺失」或反之。
- 改进：抽出统一的 `parse_uri_to_struct(uri) -> dict`（含 type/server/port/uuid/password/sni/network/security 等规范字段），两模块共用，仅在输出阶段做格式转换。

**H2. 动态阈值与「≥65分」标签/统计不一致**
- 位置：`update_nodes.py` `adjust_score_threshold`(L777-792) + `generate_final_stats`(L873) + `generate_source_stats`(L905)。
- 影响：`valid_lines = score >= threshold`，而 `threshold = max(40, min(75, int(avg*0.5)))`（最低 40）。但 `s1.txt` 注释写「所有有效节点（≥65分）」，且 `generate_source_stats` 的「低分(<65分)」分支注释称「理论上不会出现，已过滤」——**当平均分偏低使阈值落到 40~64 时，这些节点会被写入 s1.txt 却与标签/统计假设矛盾**，下游与人工阅读都会被误导。
- 改进：要么把阈值下限抬到 65（与标签一致），要么把阈值作为变量传入统计/标签文案，消除硬编码的「65」假设。

**H3. IPv6 节点被静默丢弃**
- 位置：`update_nodes.py` `probe_proxy_handshake`(L447 `socket.gethostbyname(ip)`)、`test_node_final`(L489 同)；`is_ip`(L176) 仅匹配 IPv4。
- 影响：`gethostbyname` 不支持 IPv6，含 IPv6 地址的节点在 DNS 步抛异常 → 判定 `dns_fail` → 直接丢弃。不是崩溃，但是**功能缺口**（越来越多节点用 IPv6）。
- 改进：改用 `socket.getaddrinfo(ip, None, socket.AF_UNSPEC)` 并区分 IPv4/IPv6 建连；`is_ip` 同时识别 IPv6。

### 🟡 中 / Medium

**M1. `verify_cn/.env`（含 GITHUB_PAT）未加入 .gitignore，误提交即泄漏**
- 位置：`.gitignore` 无 `verify_cn/.env`；`set_schedule.sh`(L57-58) 会 `source verify_cn/.env`。
- 影响：容器或本机若执行 `git add .`/`git add -A`，PAT 会被提交进公开仓库。
- 改进：在 `.gitignore` 追加 `verify_cn/.env` 与 `verify_cn/run_loop.sh`；并建议 `.env` 仅放容器持久盘，不要进仓库目录。

**M2. `update_nodes.py` 抓取遇 SSL 错误静默降级为 `verify=False`（MITM 风险）**
- 位置：`update_nodes.py` L642-652（默认 `verify=True`，捕获 `SSLError` 后改 `verify=False` 重试）。
- 影响：对公开订阅源的 TLS 校验被悄悄关闭，存在中间人篡改节点内容的风险（虽仅影响订阅质量，非凭据）。
- 改进：改为可配置项（如 `CONFIG["request"]["allow_insecure"]=False`），默认不降级；确需降级时显式打 WARN 并限制仅对已知源。

**M3. 废弃的 FunctionGraph 路线代码仍被跟踪（仓库污染 / 维护者困惑）**
- 位置：`verify_cn/index.py`、`verify_cn/verify_proxy_core.py`、`verify_cn/build_package.py`、`verify_cn/s.yaml`、`HUAWEI_DEPLOY.md`、`LOCAL_VERIFY.md`、`README.md` 大量描述旧路线。
- 影响：`_cdp_*.py` 已被 gitignore，但 `index.py`/`verify_proxy_core.py`/`build_package.py`/`s.yaml` **仍被跟踪**。新维护者难辨哪条是活路径；`requirements.txt` 等引用也会误导。
- 改进：将整套 FC 路线移入 `verify_cn/legacy_fc/`（并 gitignore），或在文件头统一标注 `# DEPRECATED — 已被 run_local.py(mihomo) 取代`，避免误改。

**M4. `make_proxy_names_unique` 重复实现**
- 位置：`fetch.py` L217-238 与 `merge_subs.py` L365-387 几乎逐字相同。
- 影响：命名去重逻辑若在一处置优化（如支持更多边界），另一处不会同步。
- 改进：抽到 `verify_cn/` 或仓库根的共享 `util.py`。

**M5. `update_nodes.py` 性能逼近 CI 10 分钟上限**
- 位置：`process_nodes_final`(L825-853) 每节点做 DNS+TCP探测+TLS握手，8 线程；节点数增长时总耗时上升。
- 影响：若某次源站给到几百+节点，可能撞 `timeout-minutes: 10` 被取消。
- 改进：① 提高 workflow `timeout-minutes` 到 15~20；② 对 `probe_proxy_handshake` 做抽样（仅对 TCP 通过者抽测，而非全量握手）；③ 按源并发上限细化。

**M6. `.cache/` 未被 gitignore**
- 位置：`update_nodes.py` 写 `.cache/`（L617-632）；`.gitignore` 缺该项。
- 影响：CI 每次全新 workspace 实际用不到缓存，但本机 `git add .` 会把缓存文件提交进仓库。
- 改进：`.gitignore` 追加 `.cache/`。

### 🟢 低 / Low

- **L1. `update_nodes.py` 未使用导入**：`import binascii`(L7)、`from urllib.parse import unquote, urlparse`(L13, `urlparse` 未用)。→ 删除。
- **L2. `fetch.py` 裸 `except:`**：L97、L156、L207 用 `except:`（吞掉 `KeyboardInterrupt`/`SystemExit`）。→ 改为 `except Exception`。
- **L3. `fetch_tuijian.py` 无 `__main__` 守卫 + 孤儿输出**：脚本在 import 时即执行（L11-52）；`history_tuijian.txt` 每次追加、永不清理（L51）；`latest_tuijian.txt`(L46) 写入后**从未被 `merge_subs.py` 消费**（合并源只有 s.txt/s1.txt/s2.txt）→ 死文件。→ 加 `if __name__=="__main__": main()`；清理历史文件策略；确认该脚本是否还需保留。
- **L4. `update_nodes.py` 重复解析**：`dedup_nodes_final`(L570) 与 `process_single_node_final`(L538) 对同一行各 `parse_node` 一次；可对解析结果做缓存/复用。
- **L5. `update_nodes.py` `log_msg` 死参数**：`line` 形参在全部调用中固定传 `""`（L130-132），逻辑恒为假分支。→ 简化删除。
- **L6. `calculate_node_score` 冗余 clamp**：L526 与 L532 两次 `max(score,0)`。→ 合并。
- **L7. `clash_template.yaml` 直接带 `mixed-port:7890`/`external-controller`/`allow-lan:true`**：作为「订阅导入」通常无害，但若有人误把 `s-clash.yaml` 当独立配置直接运行，会绑定端口/开 LAN。→ 在模板注释明确「本文件仅作订阅导入，勿直接运行」。
- **L8. 无根 `requirements.txt`**：各 workflow 硬编码 `pip install requests pyyaml beautifulsoup4`，且 Python 版本不一致（3.12 / 3.10 / 3.10）。→ 增加 `requirements.txt` 并锁定版本，提升可复现性。
- **L9. `run_loop.sh` 首轮延迟 15 分钟**：`sleep 900` 写在循环体最前（set_schedule.sh L54）。→ 可选：先跑一次再 sleep，使容器启动后立即有首份验证。

---

## 3. 按优先级排列的改进建议

| 优先级 | 项 | 动作 | 预计工作量 |
|--------|----|------|-----------|
| **P0** | M1 | `.gitignore` 追加 `verify_cn/.env`、`verify_cn/run_loop.sh`、`.cache/`；确认 `.env` 不在仓库 | 5 分钟 |
| **P0** | M2 | `update_nodes.py` 抓取默认不降级 TLS，降级改为显式配置 | 15 分钟 |
| **P1** | H2 | 统一阈值下限与标签/统计文案（消除「≥65」硬编码假设） | 20 分钟 |
| **P1** | H3 | `getaddrinfo(AF_UNSPEC)` 支持 IPv6 节点 | 30 分钟 |
| **P1** | M3 | 归档/标注废弃 FunctionGraph 路线代码 | 20 分钟 |
| **P2** | H1 | 抽出统一 URI 解析模块，双管道共用 | 1~2 小时 |
| **P2** | M4 | 抽 `make_proxy_names_unique` 到共享 util | 15 分钟 |
| **P2** | M5 | 提升 CI timeout + 抽样探测，缓解性能上限 | 30 分钟 |
| **P3** | L1–L9 | 清理未用导入、裸 except、孤儿文件、文档注释等 | 各 5~15 分钟 |

---

## 4. 已确认的良好实践（保持）

- ✅ `run_local.py`：子进程隔离进 `tempfile` 工作目录、`finally` 中 `terminate→wait→kill` 并 `rmtree`；本地 API 请求强制 `ProxyHandler({})` 绕开系统代理；推送带 `fetch+merge -X ours` 重试，健壮处理 non-fast-forward（与本次 workflow 修复互补）。
- ✅ `tag_verified.py`：精确按名匹配、对漂移节点告警跳过、对缺失源/模版优雅降级，无凭空造节点。
- ✅ `merge_subs.py`：去重键设计合理、`make_names_unique` 用「已占用名集合」而非计数器（规避上游 `_N` 后缀撞名，注释清晰）。
- ✅ CI 推送竞争已修复（本次提交 `ca4941d`）：两个 workflow 均在 `push` 前 `fetch+merge origin/main`，消除与容器 `verified.json` 推送的并发冲突。
- ✅ 密钥管理合规：PAT 走 `${{ secrets.PAT_TOKEN }}` 与运行时环境变量，全仓 `.py`/`.yml` 密钥模式扫描**零命中**，无硬编码凭据。
- ✅ `.gitignore` 已覆盖新路线产物（`_cdp_*`、`logs/`、`mihomo`、`analysis_tmp/` 等）。

---

## 5. 立即行动建议（最小风险、最高收益）

1. **改 `.gitignore`**（P0，防 PAT 泄漏）：追加
   ```
   verify_cn/.env
   verify_cn/run_loop.sh
   .cache/
   ```
2. **修 H2 标签不一致**（P1，消除误导）：把 `generate_final_stats` 的 `s1.txt` 描述与 `generate_source_stats` 的「理论上不会出现」注释，改为引用真实 `threshold` 变量。
3. **标注废弃代码**（P1，防误改）：在 `index.py`/`verify_proxy_core.py`/`build_package.py` 顶部加 `# DEPRECATED`。

> 以上 1、3 为纯配置/注释改动，零运行风险，可立即实施；是否需要我直接动手？
