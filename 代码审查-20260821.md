# mkdy 项目整体流程与核心逻辑审查报告

> 审查日期：2026-08-21
> 审查范围：8 个核心源码文件（fetch_tuijian.py / fetch.py / update_nodes.py / node_parse.py / merge_subs.py / name_util.py / verify_cn/run_local.py / verify_cn/tag_verified.py）+ 2 个工作流（update-subs.yml / verify-tag.yml）
> 审查目标：模块调用关系与数据流连贯性、边界/异常/资源释放纰漏、结构/性能/可维护性/可读性优化空间

---

## 一、整体流程与数据流（已厘清）

```
fetch_tuijian.py  ──latest_tuijian.txt──┐
update_nodes.py   ──s1.txt──────────────┤
fetch.py          ──s2.txt + s2-clash-1/2.yaml──┤
                                        │
merge_subs.py  ← 读上述源 + 【优先并入 s-verified.yaml】──→ s-clash.yaml + s-clash.txt
                                        │
run_local.py (国内 mihomo 真链) ← 读 s-clash.yaml ──→ verify_cn/verified.json ──push──┐
                                                                                     │
verify-tag.yml (tag_verified.py) ← 读 verified.json + s-clash.yaml + clash_template ─┘──→ s-verified.yaml + s-verified.txt
```

数据流主干是连贯的，关键衔接点（node_parse 统一解析、verified 并入、tag 匹配）逻辑正确。下面列出发现的具体问题。

---

## 二、具体问题与优化建议

### 🔴 高：配置外部化存在"浅覆盖"隐患（update_nodes.py）

**位置**：`_overlay_config_file()`（L128-146）+ `config.yaml`
**问题**：overlay 是顶层键整体替换（`CONFIG[k] = v`）。当前 `config.yaml` 只写了少量顶层键（sources/request/...）未碰 `filter`，所以暂时安全；但**一旦有人在 config.yaml 里写 `filter:` 顶层键，会整体替换内联 `filter`（含 score_rules / private_ip / SS_VALID_CIPHERS 等）**，导致评分规则、`private_ip` 正则全部丢失，`is_private_ip()` 直接 KeyError/崩溃。这是一颗静默炸弹。
**建议**：改为【嵌套合并】——对 dict 类型的值递归 `dict.update` 而非整体替换；或对已知嵌套键（filter/detection）做 `deep_merge`。同时 `_overlay_config_file` 之后强制 `CONFIG["filter"]["private_ip"] = re.compile(...)` 已做兜底，但 `score_rules` 等无兜底，仍需修。

### 🔴 高：fetch.py 的 download_nodes 协议覆盖不全，与 node_parse 不一致

**位置**：`fetch.py:194` 的 `if line.startswith(('vmess://','vless://','trojan://','ss://','hysteria://'))`
**问题**：只识别 5 种协议，**漏掉 tuic://、ssr://、hysteria2://**。而 `node_parse.py` 已支持 8 种，且 `update_nodes` 评分也支持 tuic/ssr/hy2。结果是：米贝77/Datiya 站若提供 tuic/ssr 节点，会被 `download_nodes` 直接丢弃，数据源与解析层能力不匹配，造成"明明有节点却没进订阅"的不连贯。
**建议**：协议白名单改为引用 `node_parse._PARSERS` 的 scheme 集合（单一事实来源），或至少补上 tuic/ssr/hysteria2。

### 🟡 中：merge_subs 写 s-clash.txt 用固定 base，与 --out 不对称

**位置**：`merge_subs.py:232-239`（txt_out = os.path.join(base, "s-clash.txt")）
**问题**：`--out` 控制 yaml 输出路径，但 `s-clash.txt` 永远写到 `base` 根目录，不受 `--out` 影响。上次本地试运行就因此误写了仓库根的 `s-clash.txt`（已还原）。对正常 CI（`--out s-clash.yaml` 同目录）无碍，但作为通用工具语义不一致，易踩坑。
**建议**：`txt_out` 依据 `args.out` 推导（如 `os.path.splitext(args.out)[0] + ".txt"`），与 yaml 同目录。

### 🟡 中：update_nodes.dedup_nodes_final 排序对缺 weight 节点会 KeyError

**位置**：`update_nodes.py:591` `nodes.sort(key=lambda x: x["weight"], reverse=True)`
**问题**：所有 `node` 都来自 `{"line":..., "weight":..., "source_url":...}`（fetch_all_sources 构造），理论上都有 weight。但这是隐式契约——若未来某路径传入缺 `weight` 的 dict（如测试 fixture、手工调用），会抛 KeyError 而非优雅跳过。属边界脆弱。
**建议**：`key=lambda x: x.get("weight", 0)`，防御性取值。

### 🟡 中：fetch_tuijian.py 仍用废弃 API + 强耦合第三方站点结构

**位置**：`fetch_tuijian.py:50` `datetime.utcnow()`（Python 3.12 起弃用，3.13 会告警）；且整脚本为一次性过程脚本（顶层裸跑、无函数封装、无异常粒度控制）。
**问题**：① `utcnow` 弃用；② 单站结构变更即整体失效且难以复用/测试；③ 与 update_nodes/fetch.py 风格不统一（后两者有 main()/函数化）。
**建议**：`datetime.now(timezone.utc)`；把抓取逻辑包成 `fetch_latest_link()` 函数，与项目其余脚本风格对齐，便于单测。

### 🟡 中：update_nodes 全局可变状态跨运行不重置

**位置**：`_PROBE_COUNT = {"n": 0}`（L342）、`_IPINFO_QUOTA = {"used": 0, ...}`（L345）
**问题**：这两个计数器模块级定义，单次进程内正确（每 run 一个进程）。但若将来以【常驻/多次调用 main()】方式运行（如 schedule 库定时调用同一进程），计数不重置 → 握手探测/配额永久耗尽，节点永远拿不到 `outside_ok` 加分。属"运行模型假设"隐患。
**建议**：把计数器放进 `main()` 作用域或封装为可重置的对象，避免模块级可变全局。

### 🟢 低：is_cn_ip 线性扫描 10830 条 CIDR，每节点 O(N)

**位置**：`update_nodes.py:221-224` `for net in CN_RANGES: if addr in net`
**问题**：CN_RANGES 约 1 万条，每次 `is_cn_ip` 线性扫描。update_nodes 对每个节点调多次（`dns_resolve` 里对每个候选 IP 调一次），节点量上千时累计开销可观（虽在 TCP 超时量级下不显著，但属可优化点）。
**建议**：用 `ipaddress` 预先构建按版本分桶 + 排序的网络列表，二分查找；或引入 `pyramid`/`cidr-tree`。优先级低，仅作性能提示。

### 🟢 低：异常吞没过于宽泛，不利诊断

**多处**：`b64_safe_decode`/`decode_b64_sub`/`_udp_resolve_a`/`download_nodes` 的 `except: pass` / `except Exception: return []` 吞掉全部异常无日志。
**问题**：解析失败、网络抖动、数据格式异常都被静默吞掉，CI 日志里看不到"为什么某源少了一批节点"。
**建议**：至少 `LOG.debug` 记录被吞的异常类型/长度，便于排查源站格式漂移。

### 🟢 低：fetch_source_data 缓存无并发写保护

**位置**：`update_nodes.py:632-717`
**问题**：`.cache/` 按 url md5 命名，多线程 `fetch_all_sources` 下不同源写不同文件，基本无冲突；但同一 url 重试时可能覆盖。当前规模无碍，仅记录。

---

## 三、调用关系与数据流的连贯性结论

| 检查项 | 结论 |
|---|---|
| 模块间调用关系 | ✅ 清晰：node_parse 为单一解析入口，merge_subs / update_nodes 共用；verify_cn 链路自洽 |
| 数据流方向 | ✅ 单向无环：抓取→合并→验证→打标，无反向依赖 |
| 已修复的连贯性痛点 | ✅ SSR 解析 bug（曾静默丢全部 SSR）、verified 并入去重、tag 名称精确匹配 |
| 残留不连贯 | ⚠️ fetch.py 协议覆盖 < node_parse（tuic/ssr/hy2 被数据源丢弃，见高/中项） |
| 异常处理 | ⚠️ 部分过宽（见低项），但核心路径（评分/探测）已做降级保护，不会误杀节点 |
| 资源释放 | ✅ run_local.py 的 mihomo 进程 `finally` 中 terminate/kill + 临时目录 rmtree；socket 均用 with 上下文 |
| 配置外化 | ⚠️ 浅覆盖隐患（见高项），需改为嵌套合并 |

---

## 四、优化空间优先级清单

| 优先级 | 项 | 改动量 | 风险 |
|---|---|---|---|
| P0 | config.yaml 嵌套合并（防 filter 整体丢失） | ~10 行 | 低 |
| P0 | fetch.py download_nodes 补全 tuic/ssr/hy2 协议 | ~3 行 | 低 |
| P1 | merge_subs s-clash.txt 路径与 --out 对齐 | ~2 行 | 低 |
| P1 | dedup_nodes_final 防御性 .get("weight",0) | ~1 行 | 低 |
| P2 | fetch_tuijian 函数化 + utcnow 替换 | ~30 行 | 低 |
| P2 | 跨运行计数器重置（去模块级全局） | ~15 行 | 中（需重构调用） |
| P3 | is_cn_ip CIDR 二分查找 | ~20 行 | 低 |
| P3 | 宽泛 except 补 debug 日志 | 散点 | 低 |

---

## 五、总结

整体架构合理、主干数据流连贯、核心降级保护到位（GitHub 侧探测只加分不硬过滤、验证失败不凭空造节点）。**最值得立即修的两处**：① config.yaml 浅覆盖会让 `filter` 配置整体丢失（静默炸弹）；② fetch.py 数据源漏抓 tuic/ssr/hysteria2，与已支持的解析层能力不匹配，造成实际节点流失。两者都是"现在不出错、但迟早踩雷/已悄悄漏节点"的类型，建议优先处理。
