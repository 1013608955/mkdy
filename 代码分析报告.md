# mkdy 代码安全与优化分析报告

> 审查范围：仓库全部 8 个 Python 源文件（fetch_tuijian.py / fetch.py / name_util.py / node_parse.py / merge_subs.py / update_nodes.py / verify_cn/run_local.py / verify_cn/tag_verified.py）及配套配置、CI 工作流。
> 审查时间：2026-08-18（基于 `origin/main` 最新 tip）。
> 严重程度：🔴 高（安全泄漏/崩溃/挂起）｜🟡 中（逻辑错误/健壮性/正确性）｜🟢 低（风格/文档/小边界）。

---

## 一、漏洞与缺陷分析（按文件维度）

### 1. `fetch_tuijian.py`（推荐订阅抓取）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| 订阅直链落盘未隔离 | L46-52 | 🟡 | `latest_tuijian.txt` / `history_tuijian.txt` 写入真实订阅直链，且**未被 `.gitignore` 忽略**，误提交即泄漏订阅。 |
| `datetime.utcnow()` 已废弃 | L50 | 🟢 | Python 3.12+ 该 API 废弃，未来版本将告警/移除。改用 `datetime.now(timezone.utc)`。 |
| 站点改版即静默失败 | L21-41 | 🟢 | 匹配逻辑依赖 `tosslk.xyz` + 长度≥32 的脆弱正则，页面结构一变就 `exit(1)`，无重试/兜底源。 |
| 无函数封装 | 全文 | 🟢 | 逻辑全在模块顶层，无法 import 复用、无法单测。 |

### 2. `fetch.py`（米贝77 / Datiya 节点源抓取）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| 订阅产物未 gitignore | 产出 s2.txt / s2-clash-1.yaml / s2-clash-2.yaml | 🔴 | 这些 base64 节点订阅产物**不在 `.gitignore`**，本地跑后极易被 `git add .` 误提交泄漏。 |
| 裸 `except:` | L98 | 🟡 | Datiya 分支 `except:` 吞掉 `KeyboardInterrupt`/`SystemExit`；`strptime` 失败静默 `continue`，难排查。 |
| 日期筛选与注释不符 | L95 | 🟡 | ` (now - article_date).days <= 1` 只取 1 天，注释却写"近2天"；且阈值过窄易漏最新文章。 |
| 整页正则无上限 | L129-143 | 🟢 | 对整页 HTML 用 `findall(r'https?://...')` 等，大页面有轻微内存/ReDoS 风险；`set()` 去重丢失顺序。 |
| 错误处理吞错 | 多处 `except Exception: print; return []` | 🟢 | 失败时难以定位根因；建议统一 logging + 结构化错误。 |
| 明文 URI 误当 base64 | L188 | 🟢 | 当 `source` 非 http 时直接 `base64decode`，若传入含 `:` 的明文 URI 会失败返回 []（当前调用链不触发，但边界脆弱）。 |

### 3. `name_util.py`（名称去重工具）
- 整体质量良好，仅一处极低风险：空名回退 `{type}_{server}:{port}` 可能与真实节点名撞车，但 `make_proxy_names_unique` 用"已占用集合"兜底，不会 fatal，可接受。

### 4. `node_parse.py`（统一 URI 解析 + 反向转换）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| `struct_to_uri` SSR 丢字段 | L392-394 | 🟡 | SSR 仅输出 `cipher:password@host:port`（origin/plain），**丢失 obfs/protocol**，v2rayN 导入后可能连不上。 |
| `struct_to_uri` 硬编码参数 | L396-411 | 🟡 | tuic/hysteria2 的 `congestion_control=bbr` / `alpn=h3` / `udp_relay_mode=native` 为**写死猜测值**，非来自节点 → 与原始不符，保真度低。 |
| hysteria 协议被改写 | L267 / L396 | 🟡 | `hysteria://` 原节点统一输出 `hysteria2://`，scheme 被改写（注释已说明，但 v2rayN 侧语义变化）。 |
| `b64decode` 吞异常 | L19-26 | 🟢 | 所有异常返回 `""`，解析失败难定位。 |
| `_parse_ss` 含 `@` 的 userinfo | L176-184 | 🟢 | userinfo 含 `@` 时 fallback 分支可能误解析；`plugin` 仅透传未校验。 |

### 5. `merge_subs.py`（合并去重 → s-clash.yaml / s-clash.txt）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| **fallback 缺 `return` 会崩溃** | L174-182 | 🔴 | 模板缺失时写出无模版 yaml 后**未 `return`**，继续到 L182 `open(template_path)` → `FileNotFoundError` 崩溃。"优雅降级"实际不成立（当前 CI 模板恒在，故未触发）。 |
| 两产物节点数不一致 | L213-222 | 🟡 | `struct_to_uri` 对 anytls 等返回 None → `s-clash.txt` 节点数 < `s-clash.yaml`，集合不一致（设计权衡，需文档说明）。 |
| `decode_sub` 重复扫描 | L53-82 | 🟢 | 先整文件 b64 解码（可能很大）再逐行解，大文件重复工作；失败静默。 |
| `clean_node` 删 `tls:False` 之外的值 | L112-119 | 🟢 | `tls: False` 布尔假被保留（合理），但端口 0 被删（合理）；无显式校验。 |

### 6. `update_nodes.py`（节点抓取+评分，重点模块）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| **`_resolve_connect` DNS 无超时** | L197 | 🔴 | `socket.getaddrinfo` 无超时；节点 `address` 为域名且 DNS 卡住时，**worker 线程永久阻塞**（系统解析器在 Windows 可长达数十秒+），可能拖垮整轮处理。对比 `dns_resolve` 已用线程+join(timeout) 保护，此处遗漏。 |
| `is_cn_ip` CN 段严重不全 | L65-75 | 🟡 | 缺失 36/39/42/45/49/58-61/110-119/125/175/192-198/218/219 等大量 CN 段 → 大量国内节点被误判"境外"，CN 惩罚(-5)/拦截失效，背离"过滤国内节点"目标。 |
| 每节点 DNS 解析两次 | process_single_node_final / test_node_final | 🟡 | domain 走缓存 `dns_resolve`，address 走 `_resolve_connect`（无缓存、无超时）→ 重复且危险。 |
| `dns_ok` 反映错误对象 | L509 | 🟡 | `dns_ok = dns_resolve(domain)` 用 `domain=sni/peer`，而 `test_node_final` 实际连 `address`，二者可能不同域名 → dns_ok 语义错位。 |
| 巨型 CONFIG 硬编码 | L25-100 | 🟡 | 8 个外部订阅源 URL + 评分权重 + 过滤规则全写死在代码中央，无外部化；改源/调权重需改代码、重新发版。 |
| `decode_b64_sub` 误判 | L154-169 | 🟡 | 整文件 base64 判定条件（`len%4==0` + 字符集匹配）可能把恰好满足的纯文本/JSON 误当 base64 解码 → 噪声节点。 |
| 注释与值不符 | L40 / L93 等 | 🟢 | `tcp_retry` 注释"从 3→2"但值为 1；多处注释与实际配置漂移。 |
| `calculate_node_score` 9 位置参数 | L477 | 🟢 | 参数顺序脆弱，调用方易错（历史已踩）；建议改 dataclass/kwargs。 |
| 零单元测试 | 全文 | 🟢 | 全仓无测试，H1 引入的 `node_parse` 无回归守护。 |
| 探测禁用证书校验 | L412-414 | 🟢 | `CERT_NONE` + 不校验 hostname，属探测用途可接受，但应注释"仅探测、不用于真实流量"。 |

### 7. `verify_cn/run_local.py`（本机 mihomo 真链验证）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| 日志被截断 | L267 | 🟡 | `mihomo.log` 每次 `open(...,"w")` 覆盖，丢失历史排查线索；建议追加或轮转保留 N 份。 |
| 子进程孤儿风险 | L308-314 | 🟡 | 脚本被 SIGKILL 时 `finally` 不执行 → mihomo 子进程可能成孤儿占用端口；建议注册 atexit/信号 handler。 |
| `generated_at` 用 UTC | L318 | 🟢 | 与本项目"北京时间"约定不一致，小问题。 |
| 用 `print()` 而非 logging | 全文 | 🟢 | 与 update_nodes 的 logging 风格不统一。 |

### 8. `verify_cn/tag_verified.py`（打标生成 s-verified）
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| 输出顺序漂移 | L75 / L87 | 🟢 | `selected` 顺序沿用 `verified.json` 的 ok 列表，而非 s-clash 稳定排序 → s-verified.yaml 每次顺序漂移（diff 噪声）。 |
| `load_ok` 静默空集 | L44-55 | 🟢 | verified.json 损坏时返回空集，上游已处理，影响低。 |

### 9. 跨模块 / 配置
| 项 | 位置 | 严重度 | 说明 |
|---|---|---|---|
| **订阅产物/链接未 gitignore** | .gitignore | 🔴 | `s.txt`/`s1.txt`/`s2.txt`/`s2-clash-*.yaml`/`latest_tuijian.txt`/`history_tuijian.txt` **均未忽略**，本地跑后极易误提交 → 泄漏真实订阅/直链。建议立即补 `.gitignore`。 |
| 无依赖清单 | 仓库根 | 🟡 | 缺 `requirements.txt`/`pyproject.toml`，依赖(requests/bs4/pyyaml)隐含，环境不可复现。 |
| 自反馈环脆弱 | update_nodes ↔ merge_subs | 🟡 | update_nodes 产出 `s1.txt` 又被 merge_subs 当源，链路脆弱；两模块对同一节点重复解析 2~3 次。 |

---

## 二、优化空间分析

### A. 架构设计
- **集中配置外部化**：`update_nodes.CONFIG`（~75 行）应抽到 `config.yaml`/`config.json`，订阅源 URL 列表独立，改源/调权重零代码改动。
- **统一中间模型、一次解析**：当前 fetch→dedup→process 三阶段各 `parse_node` 一次（共 2~3 次）。建议在 fetch 阶段解析为 `node_parse` struct 并缓存，后续阶段只消费 struct，消除重复解析与字段漂移。
- **明确自反馈环职责**：`s1.txt` 作为 update_nodes 输出又被 merge 输入，应在文档/接口层界定清楚，避免循环依赖意外。
- **验证后端抽象**：`run_local.py` 绑定 mihomo，可抽 `VerificationBackend` 接口，便于将来换 sing-box / 远程验证器。

### B. 性能
- **`_resolve_connect` 加超时 + 复用缓存**：这是最大性能/稳定性隐患（见漏洞 §6）。应复用 `dns_resolve` 的线程+超时机制，并对 connect 设 `socket.settimeout`，避免单点 DNS 卡死整轮。
- **`is_cn_ip` 改写**：弃用手写正则，改用 `ipaddress` + 维护良好的 CN 段集合，或引入 GeoIP 数据文件，提升正确性与可维护性。
- **`decode_sub` 减负**：先判明文行、再整体 b64，减少大文件二次全扫描。
- **并发度可配置化**：`thread_pool`、fetch workers 已参数化，但应与节点规模联动（大规模时自动扩容）。

### C. 可维护性
- **统一日志**：`fetch.py` / `run_local.py` 仍用 `print()`，应统一到 `logging`（update_nodes 已做），便于级别控制与排查。
- **依赖与工具链声明**：补 `requirements.txt` + `pyproject.toml`（含 ruff/black/pytest），CI 加 lint+test 门禁。
- **`calculate_node_score` 签名改造**：9 个位置参数改为 `dataclass`/`kwargs`，消除顺序脆弱（历史已踩坑）。
- **补全 `.gitignore`**：把订阅产物/链接文件、`.env`、`*.log` 等全部纳入，杜绝误提交泄漏。
- **复用公共工具**：`fetch.py` 内联的 b64/正则可复用 `node_parse.b64decode`，减少重复实现。

### D. 可扩展性
- **协议解析注册表已就绪**：`node_parse._PARSERS` 是良好扩展点，新协议只需加 `parse_*` + `struct_to_uri` 两项，无需改动调用方。
- **订阅源配置驱动**：新增抓取源只需改配置（fetch.py 的 `TARGET_SITES` / update_nodes 的 `sources`），无需改逻辑。
- **输出格式可插拔**：`struct_to_uri` 已支撑 v2rayN；将来若要输出 sing-box / surge 格式，在 `node_parse` 加对应 `struct_to_xxx` 即可，merge/tag 仅需调用新函数。

### E. 安全加固（优先级建议）
1. 🔴 立即把 `s.txt`/`s1.txt`/`s2.txt`/`s2-clash-*.yaml`/`latest_tuijian.txt`/`history_tuijian.txt` 加入 `.gitignore`（防泄漏）。
2. 🔴 修 `merge_subs.py` fallback 缺 `return`（防 CI 模板异常时崩溃）。
3. 🔴 给 `_resolve_connect` 加 DNS/connect 超时（防线程挂起）。
4. 🟡 补全 `is_cn_ip` CN 段（恢复"过滤国内节点"功能）。
5. 🟡 `struct_to_uri` 补回 SSR 的 obfs/protocol、tuic/hysteria2 真实参数（提升 v2rayN 节点可用性）。
6. 🟢 补 `requirements.txt` + 最小 pytest 回归（守护 H1/H3 等已落地改造）。

---

## 三、优先级行动清单（建议顺序）

| 序 | 动作 | 严重度 | 预估改动 |
|---|---|---|---|
| 1 | `.gitignore` 补订阅产物/链接文件 | 🔴 | 1 行 |
| 2 | `merge_subs.py` fallback 加 `return` | 🔴 | 1 行 |
| 3 | `_resolve_connect` 加超时+复用 DNS 缓存 | 🔴 | ~15 行 |
| 4 | 补全 `is_cn_ip` CN 段（或 GeoIP） | 🟡 | 中 |
| 5 | `struct_to_uri` 保真修复（SSR/tuic/hy2） | 🟡 | ~30 行 |
| 6 | `update_nodes.CONFIG` 外部化为 YAML | 🟡 | 中 |
| 7 | 补 `requirements.txt` + pytest 回归 | 🟢 | 小 |
| 8 | 统一 logging / 消除重复解析 | 🟢 | 中 |

> 注：以上为静态审阅结论，部分项（如 `_resolve_connect` 挂起、`is_cn_ip` 误判）建议结合真实运行日志（verify_cn/logs、CI run log）二次确认影响面后再修。
